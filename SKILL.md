---
name: museum-stamp-print
description: Turn a supplied photo into one museum-collection-style multi-layer registration stamp sheet (套版印刷分版样张). The scene is reduced to five flat geometric ink layers plus one dark keyline layer, slightly misregistered like real block printing; the sheet displays the finished overprint and each same-size plate separation side by side, like an authentic printmaker's proof sheet. Use when the user mentions 套版印刷, 套色印章, 纪念章, 博物馆印章, 文创印章, 分版, stamp print, block-print seal, or asks to turn a travel or memory photo into a layered stamp design. Generates the image, not prompt-only.
agent_created: true
---

# Museum Stamp Print

Turn one user photo into ONE printmaker's registration proof sheet (套版分版样张), generated by the built-in image generation tool (WorkBuddy: `ImageGen`; pass local photo paths via the `image1` parameter for image-to-image). Never simulate the print with code, filters, or SVG.

The sheet mimics a real multi-block print's proofing record: the finished overprint plus each plate's separation, all at IDENTICAL size and alignment — overlaying the plates would reconstruct the finished print.

Work autonomously through all steps; stop only if image generation is unavailable. Regenerate at most once if a quality check fails; otherwise deliver and report deviations honestly.

## Mental model lock (the #1 drift cause — verified 2026-09-14)

The proof sheet shows **INK ON PAPER** — the impression left after pressing, NOT the physical stamp itself. The model's natural tendency is to render "a rubber stamp / a stamp block / a carved seal" as a 3D object: drop shadows, paper thickness, relief, cut-card edges, sticker outlines. This is the source of every paper-cut / cardstock / 3D-relief drift in earlier runs.

Lock the rendering target with this rule in EVERY prompt:

- The image contains **only ink sitting flat on paper**. The stamp press, the rubber block, the carving, the ink pad, the hand — NONE of these exist in the picture.
- **No shape is ever cut out, punched out, or pasted onto another shape** (user correction 2026-09-14, dog+child photo). Ink is **deposited onto the existing paper surface**. The dog's ink lands on top of the ground ink as a translucent multiply layer (where they overlap, the tone reads slightly darker than where the dog sits on bare paper) — there is NO white edge, NO halo, NO separation line between the dog and the ground; the dog is simply where more ink was applied on top. The child's reserved area is where the ground ink simply **did not reach** — bare paper, the same paper as the margins, with no special outline, no white gap, no cut-line.
- Use impression-level vocabulary: "ink impression on paper", "stamp impression", "ink mark", "ink left by a press", "freshly-inked impression", "museum check-in impression". These anchor the model on the result.
- AVOID object-level vocabulary in the render description: "a rubber stamp", "a stamp block", "a carved seal", "a stamp object", "the stamp", "the rubber block". These trigger the model to draw the physical object. Also avoid "cut out", "punched out", "pasted on top of", "stamped over", "white outline around", "halo around" — anything that frames one shape as a separate piece layered above another.
- **TOTAL BAN inside the image prompt (user correction 2026-09-14, third offense):** "block" / "plate" / "rubber" / "raised" / "pressed" / "carved" must NEVER appear anywhere in the image-generation prompt — not even as workflow framing, not even in per-cell headers. Verified: per-pull phrasing like "block N has only shape X raised; pressed on top of the previous result" re-triggered object rendering and was called out as pure misdirection. "Block"/"plate" are for OUR planning discussion with the user only. Inside the prompt there is no block, no plate, no press, no raised part — there is only **"one new inking" / "one more color of ink deposited onto the paper"**. Cell headers read "Impression 2 — everything from impression 1, PLUS one new inking: <element>", never "Cell 1 + <block name>".

## Overprint / 叠印机制

This is a hard color-behavior rule for every cell. Violating it produces the two most common failure modes seen in early runs: white outlines around figures ("cut-out / pasted" look) and watercolor-like soft edges where two inks meet.

1. **Across steps → translucent multiply**: when the ink of a later step lands on top of an earlier step, the combined area reads **darker and richer** than either ink alone. Use phrasing like "translucent ink deposited on top", "where it crosses earlier ink the tone deepens", "real overprint". Never describe it as "pasted over", "placed on top as a separate piece", or "white gap separating layers".
2. **Within one step -> butt/knockout only**: colors printed in the SAME pull do not mix. They either meet edge-to-edge (butt) or one shape leaves unprinted paper for another (knockout). Within a single impression, a color has one flat value across its entire area.
3. **No white rim / halo / cut-line**: the boundary between any two inks or between ink and bare paper is simply where one ink ends. There is NO white outline, NO luminous halo, NO cut-card separation, NO gap, NO bevel. If the prompt keeps producing thin pale edges, explicitly add: "where two inks meet they touch directly; the edge is just the end of one color and the start of another".
4. **Bare paper is NOT white ink**: the only place bare paper shows is where ink was never deposited. It is never a "white shape" or "white void" layered above another color. Describe it as "unprinted paper", "area the ink did not reach", or "paper margin".
5. **Carrying figures forward means preserving their ink**: if the dog appears in cell 2, the same dog ink must remain visible and unchanged in cell 3 and cell 4. It is not redrawn, not re-outlined, not framed again. The later cell simply has additional ink on top of it.
6. **Ink is permanent; each step can only ADD, never REMOVE or LIGHTEN (user rule 2026-09-14, dog+child photo)** — physical law of stamp impressions. Once ink is deposited on the paper it cannot be erased, lightened, washed off or made transparent by a later step. So a later step can deepen existing ink (light ink over light ink → medium; dark ink over light ink → darker) but it can never undo what an earlier step already printed. Phrase in every cell's prompt: "the ink already on the paper from earlier cells is PERMANENT — it stays exactly where it is, and this cell ONLY adds new ink on top; do NOT erase, lighten, fade, remove or make transparent any earlier ink". A light color may be darkened by overprinting a dark color (multiply), but a dark color may never become light or invisible.

## Code-layer QC / 代码层品控

Prompt engineering alone cannot force the image generator to obey the "no white rim" and "no cut-out" rules (verified 2026-09-14 on the dog+child photo). Therefore **every generated sheet must go through a code-level quality pass** before delivery. Code is allowed to alter the generated pixels for cleanup; it is NOT allowed to synthesize the print itself.

Run `python scripts/strip_rim.py <input.png> <output.png>` after generation:

- Detects bright gray-ish pixels that sit next to dark ink (the classic AI-generated white outline).
- Replaces them with the local median of non-rim neighbors.
- Tuned defaults: `bright_min=230`, `rb_max=15` (cooler than warm paper), `dark_dilate=5`.

Also perform these post-generation fixes in code when needed:

- **Watermark fill**: the bottom-right corner often carries a platform watermark. Fill it row-by-row using the paper color sampled immediately to the left of the watermark band plus small noise. Never sample from a distant corner.
- **Text repair**: if the generated text is truncated (e.g. "PRINTED FROM CHIL" instead of "PRINTED FROM CHILDHOOD"), erase the truncated band and redraw the intended text with Pillow in vintage postal capitals.
- **Numerals**: if the generated sheet drops the 1/2/3/4 numerals, add them with Pillow (Times bold, just above-left of each cell).

## Workflow

1. Inspect the source photo at full useful detail.
2. Extract: one identification anchor (the element that says WHERE/WHAT this is), the composition arrangement (keep it faithful), the light direction, and 5-6 dominant color families.
3. Decide the text content: place name (user-supplied, or the most confident recognized location; otherwise omit) and date (today by default, format `YYYY.MM.DD`; user override wins).
4. Read [references/style-guide.md](references/style-guide.md) for the layer system, registration, and figure rules.
5. Pick the proof-sheet MODE, then shape the prompt with the matching skeleton (see [Proof-sheet modes](#proof-sheet-modes--proof-sheet-modes)). Both modes share the **PER-CELL INVENTORY TEMPLATE** below — never use delta phrasing ("same as previous + X") because the model drops carried-forward elements (verified 2026-09-14, dog photo: cell 3 lost the dog). Always enumerate ALL elements each cell contains.
   - **Mode A — single-shot 2×2 (default for scenes with figures or foreground subjects that must carry forward)**: ONE prompt, ONE image, hard-coded 2×2 grid with each cell's full inventory listed, the finished master as `image1` for style anchoring. Cheapest, most consistent, no chain drift.
   - **Mode B — chained one-cell-at-a-time (only for abstract scenes with NO figures / NO foreground subjects)**: Stage A master, Stage B chain one cell at a time, Stage C Pillow compose. Drifts on figures; safe for pure color-block scenes (e.g. Okinawa landscape).
   - **Stage A — master**: generate the finished single stamp from the source photo (`image1`=photo), in the ROLLED orientation (default roll: 70% `1024x1024`, 30% non-square — see Sheet layout; portrait → `1024x1536`, landscape → `1536x1024`; user override wins). This becomes the LAST cell.
   - **Stage B — chain one cell at a time**: cell 1 = edit the master with "show ONLY the base ink shapes — remove everything else" (`image1`=master); cell k = edit cell k-1 with "keep everything exactly as it is; deposit ONE more inking on top: [layer]" (`image1`=cell k-1). **Master-anchor rule (verified 2026-09-14, dog photo): plain "keep everything" chains STILL drift cumulatively — steps ended up deviating from the source, from each other and from the finished print. ALWAYS pass the finished master as `image2` in EVERY chained cell** ("copied EXACTLY from image 2 — same position, same size, same silhouette"), and name concrete geometric anchors in every prompt (horizon height, wall edges, ladder angle, margins, void positions/scales). Each prompt names the one new inking and restates the mixing rule (translucent mixing only against EARLIER ink; colors deposited in the same inking butt or leave unprinted paper for each other). Every cell generated in the SAME size/orientation as the master.
   - **Stage C — compose with Pillow** (printing is real generation; only layout is code): per-row-sample fill each cell's bottom-right watermark; crop each cell to its print region (explicit crop boxes after visual inspection — chained cells drift in margin size, normalize so prints read the same scale); pad to the MASTER'S aspect ratio (not always square — portrait master → portrait cells) with each cell's own paper color; paste 2x2 on a paper-colored sheet (sheet aspect = 2:1-ish in the master's orientation) with subtle noise; draw numerals (Times bold) outside each cell's upper-left corner and 4 sheet-corner registration crosses with PIL. No arrows.
   - Cell N (finished) = the master itself. Chain drift makes the master differ from the chained cells — fix by REVERSE CLOSING (verified 2026-09-14): regenerate the master FROM cell N-1 ("keep the ENTIRE scene exactly; deposit the final detail inking + text on top"), optionally upgrading one element via a second reference image (`image1`=cell N-1, `image2`=old master — e.g. "replace ONLY the person with the better-rendered one from image 2, same spot and scale"). Then regenerate cell N-1 from the NEW master by REMOVING the keyline ("remove ONLY the near-black keyline linework and the text") — enumerate precisely which marks belong to the keyline vs earlier ink, or the model strips earlier-ink textures too (verified: wall stone-joint dashes got removed with the keyline; keep them by name). Numerals sit just OUTSIDE each cell's UPPER-left corner (lower-left collides with corner crosses / gutter arrows — verified).
   - **Knockout / reserved paper (verified 2026-09-14)**: at the design level, the foreground subject's area is simply left unprinted by the early inkings. At the IMPRESSION level, that area reads as **bare paper** surrounded by ink. In the prompt, phrase it as "the ground ink does not cover this area" or "this area stays as unprinted paper" — never "carved hole / 镂空 / cut-out void" as a visual descriptor, or the model draws a paper-cut edge.
   - **Mode B region-edit warning**: if you must edit an existing cell to remove ink from a reserved area, use the finished master as `image2` ("remove the grey ink ONLY inside the person's silhouette; keep everything else"), then derive the OTHER early cells from that corrected cell by adding/removing their own block. **Over-erase is the top failure mode of region edits** ("remove X, keep Y"): the model rewrites Y too (wall lost its grey fill, road vanished). Fix by a restore pass with the pre-edit version as `image2`: "restore the grey wall plane and road from image 2; keep the one improvement from image 1". Always diff region edits against their source before composing.
   - **Legacy whole-sheet routes (fallback only — they violate step logic; keep for their verified lessons)**: Stage A anchor note: generating the single stamp with `image1`=photo traces contours far better than text alone. Whole-sheet generation with the master as `image1` **Known limitations (verified 4/4)**: the model tends to COPY the finished print into most cells — accumulation is unreliable and still breaks at row transitions; numerals misprint near the end. Phrasings that help: "ACCUMULATION IS CRITICAL: … never show the finished scene early" with a nearly-empty cell 1, "the text appears ONLY in the last cell", negative space ("the ENTIRE UPPER HALF of cell 2 is STILL EMPTY paper"). Even the simplified 4-step 2x2 (verified 3/3) still merges adjacent low-contrast steps (wall+greenery together, near-identical neighbors, early subject). TEXT-ONLY whole-sheet accumulates more reliably but traces contours loosely, and with `image1`=photo some cells render photographically (2/2 failures).
6. Watermark-check BOTH stages (bottom-right corner): image platforms often stamp a faint watermark there. If it shares the bottom band with numerals/arrows that must survive, ERASE it by filling the watermark rectangle with paper color plus slight noise (Python/Pillow) instead of cropping; otherwise crop. **Fill-color lesson (verified)**: never sample paper color from a distant corner — sample PER-ROW from the pixels immediately LEFT of the fill region and use their median; a single global sample leaves a visibly mismatched rectangular patch. Save under a clean filename and delete the watermarked original.
7. Quality-check the sheet (see below). Regenerate once if needed.

## Proof-sheet modes

The mode is chosen once at the top of a run, BEFORE writing any prompt. It does not change between cells.

### Mode A — single-shot 2×2 (default for scenes with figures or foreground subjects)

ONE prompt, ONE image. The finished master is generated separately first (`image1`=source photo, in the rolled orientation) and is then used as `image1` of the single-shot proof-sheet prompt so the four cells inherit its ink character and palette without being forced to copy it.

The single-shot prompt is written strictly in PER-CELL INVENTORY form. Each cell section enumerates **EVERY element present in that cell** (geometry, blocks, figures, text/no-text). The template is mandatory — never write "same as previous + X" because the model will drop the carry-forward (verified 2026-09-14, dog photo: cell 3 lost the dog from cell 2).

**Impression-level language only**: the proof sheet shows STAMP IMPRESSIONS on paper, not the carved rubber blocks. When an area is "reserved" for a later inking, what the viewer sees is simply **bare paper with no ink** — not a carved hole, not a cut-out, not a white silhouette with a gap or outline. Describe it as "the ground ink does not cover this area" or "that area remains unprinted paper". Never use "carved void / 凿空 / 镂空 / hole" as a visual descriptor in the prompt, or the model draws a paper-cut edge.

**Two-subject decomposition rule (verified 3/3 runs, 2026-09-14, dog+child photo)**: when the scene has TWO foreground subjects, do NOT give them separate steps — the model cannot hold both subjects in an intermediate cell (environment → +dog → +child → details always drops one subject from the intermediate cells, and detail negatives like "no red collar yet" leak early). Instead exploit the model's copy-the-final tendency: **environment → BOTH subjects in one multi-color inking (flat base colors) → detail inking (collar, accent dashes) → text**. This order also renders the reserved-paper area in impression 1 far more reliably. Trade-off to disclose: the last transition is weak (text only), and an early detail leak (e.g. the collar appearing in impression 2) is still possible — strip it with code QC or accept it.

```
[A 2×2 PROOF SHEET on warm off-white paper: exactly FOUR stamp impressions of
the SAME print arranged in a 2x2 grid, equal size, equal margins, numbered 1 2
3 4. Each impression is a SINGLE flat image — no sub-panels, no variations,
no repeated mini-images, no comparison strips. All four impressions share
EXACTLY the same composition, geometry and scale; only the ink accumulation
differs. The scene: <one-line description anchored to image1>.]

Impression 1 — the FIRST inking, base color areas only:
  contains exactly:
  - <list every base ink shape present: ground, wall, ladder rails, rungs,
    etc. — each with its color, position, silhouette>
  - <unprinted paper areas where later inkings will sit, if any — describe
    only as "the ground/wall ink does not cover this area", with the area's
    position and shape; never "white silhouette" or "cut-out hole">
  ABSOLUTELY NO <explicit negative list: no figures, no collar, no keyline,
  no text>.

Impression 2 — everything from impression 1, PLUS one new inking: <element>:
  contains EVERY element from impression 1, in the SAME positions and SAME
  colors.
  PLUS: one new color of ink deposited on top — <exact silhouette, color,
  position, scale; where it crosses earlier ink the tone deepens
  (translucent overprint)>.
  ABSOLUTELY NO <negative list: e.g. no other figure, no collar yet, no
  keyline, no text>.

Impression 3 — everything from impression 2, PLUS one new inking: <element>:
  contains EVERY element from impression 2 UNCHANGED (re-list them, do not
  rely on "same as 2").
  PLUS: one new color of ink deposited on top — <silhouette, color,
  position, scale>.
  ABSOLUTELY NO <negative list>.

Impression 4 — FINAL: everything from impression 3, PLUS the detail inking:
  contains EVERY element from impression 3 UNCHANGED.
  PLUS: <all detail marks: collars, leashes, pattern accents, ladder rungs,
  keyline dashes, etc.>
  PLUS: the text '<chosen postmark line>' in vintage worn
  postal-cancellation capitals near the bottom.
  This is the ONLY impression containing any text.

Ink character (<Neutral / Dry / Oily>): <the style paragraph>.
Medium reminder: flat printing ink on ONE paper plane — NOT a painting
(no brushstrokes, no impasto, no canvas), NOT watercolor, NOT paper cut,
no drop shadows, no thickness, no relief.

Final self-check before drawing:
  - all four impressions share identical composition and scale
  - only impression 4 contains any text
  - every impression has a SINGLE image inside (no sub-panels)
  - no black frame, no border around the sheet or any impression
```

After the single-shot returns, run the **post-generation checklist** (below). One regeneration is allowed.

### Mode B — chained one-cell-at-a-time (only for abstract scenes with NO figures)

Stage A generates the finished master. Stage B generates cell 1 by editing the master (remove everything except under-blocks), then cell k by editing cell k-1. Stage C composes the four cells into a 2×2 with Pillow.

The chain's per-prompt delta is rewritten as a **mini inventory** (still short per prompt, but lists what must remain — the previous cell's blocks and silhouettes must be re-named as "keep these: ..." rather than "keep everything"). Cells must use the finished master as `image2` so the model can copy silhouettes back from it.

Chain mode still drifts on figures — use Mode A the moment a foreground subject (person, animal, vehicle, anything that must persist across cells) appears in the scene.

### Post-generation checklist (applies to both modes)

1. **Composition constancy**: the four cells share the same horizon / wall edges / subject positions / scale. If not, the mode failed; switch or regenerate.
2. **Carry-forward completeness**: every element from cell k-1 is present in cell k at the same position and scale. If an element vanished (e.g. dog gone in cell 3), the inventory was incomplete — list the dropped element EXPLICITLY in every later cell's prompt and regenerate.
3. **Text discipline**: only the last cell carries text. If cells 1-3 contain text, the "ABSOLUTELY NO text" line was not strong enough — move it to the top of every cell section.
4. **Single-image per cell**: each cell shows ONE impression. If any cell returns a 2×2 sub-grid, the prompt triggered "before/after" or "variation" mode; prepend "no sub-panels, no variations" at the very top of the prompt.
5. **Style consistency**: ink character (Neutral / Dry / Oily) reads the same in every cell.
6. **White rim / cut-line around figures (verified 2026-09-14, dog+child photo)**: even with "NO white edge / NO halo / NO cut-line" instructions, the model tends to draw a thin light rim around newly added shapes, making them look pasted on. If this appears, the prompt's edge framing was still too object-like. Reframe the edge as a process: "the dog's brown ink simply ends and the ground ink continues right up to that edge from below", "the two inks butt at a shared boundary with no paper showing between them", "the shape is where more ink was deposited, not a separate piece". Add "seamless butting edge" and "no visible boundary line" language. If the rim persists after one regeneration, accept it as a model limitation and note it in the delivery.

## Sheet layout (the core)

- Canvas ORIENTATION — all three supported: square, portrait, landscape (user rule, 2026-09-14). **Default is a RANDOM roll per run: 70% square (`1024x1024`), 30% non-square** (roll once at the start — e.g. `random.random() < 0.7` → square). When non-square: portrait source photo → `1024x1536`, landscape source photo → `1536x1024`; square source photo rolled non-square → pick the orientation that suits the composition. User override always wins (explicit "竖版/横版/方形" beats the roll). Whichever orientation is chosen, keep EVERY generation (master + all chained cells) in that same size/orientation. The 2x2 step grid stays; only the cell aspect changes (2x2 portrait cells compose a portrait-ish sheet, 2x2 landscape cells a landscape sheet). Report the rolled orientation in the delivery notes.
- **Default layout (user preference, verified): SIMPLE 4-STEP diagram, 2x2 grid** — exactly 4 squares of IDENTICAL size, strict reading order, building BIG under-blocks first, small blocks last, with steps as EVENLY BALANCED as the model allows: cell 1 ONLY the largest base block(s) (bare paper field / field + road — nothing else), cell 2 = cell 1 + one big mid block, cell 3 = cell 2 + secondary masses (greenery), cell 4 = the FINISHED overprint (+ subject blocks, keyline, text). No separate finished-only cell — the last step IS the finished print. **The last step must be a BIG visible jump: +DETAILS +text, never text alone (user-corrected)** — the anchor garment's pattern (e.g. bingata 纹样) is a separate DETAIL BLOCK stamped in the FINAL step together with the keyline and text; the garment's earlier cell shows only its plain base color. A final step that adds only text/keyline is a failure ("变化太小"). Larger layouts (1 finished + N steps, 3x2/4x2) only when the user explicitly asks for more steps.
- **Order marks**: small bold black numerals `1`, `2`, `3`… just OUTSIDE the UPPER-left corner of each cell. No gutter arrows — in a 2×2 grid the reading order is self-evident, and arrows were found to fight with the numerals and corner crosses (user preference, verified 2026-09-14). Numerals live only outside the print area, never clipped by the canvas edge.
- The stamp rectangle is a plain RECTANGLE (sharp corners, no rounded frame, no border, no black frame): its boundary is simply the edge of the printed color areas on the paper. Nothing draws an outline around the stamp — watch for dark bottom edging and dashed underlines beneath the text (verified remnants).
- **Step logic (hard rules, user-corrected)**: the proof sheet shows IMPRESSIONS, not the carved blocks. (a) one step = one impression from one carved block — the ink from that block appears in the SAME cell as a complete layer, never split across cells; (b) translucent mixing happens ONLY where a layer crosses layers from EARLIER cells — within one cell's single pull, colors butt or separate with thin white knockout gaps and never mix with each other; (c) a cell may only show mixing with layers already stamped; (d) all prints same size — the printed area reads at the same scale in every cell.
- Tiny printer's registration cross marks (套准十字线, thin hairline crosses) may sit just OUTSIDE two corners of each rectangle — they are marks, not text, and are allowed only there.
- No other text anywhere: no titles, captions, serial numbers, logos, museum names, or decorative pseudo-text.

## Text strip

- Inside the finished print (the FINAL cell) near its bottom: short English copy in small capitals + the date, set in a **vintage postal-cancellation style** (邮戳风): slightly condensed serif or stamp-engraved letterforms, unevenly inked with light patchy fade like a partially worn rubber postmark, occasional tiny ink gaps inside strokes, baseline very slightly wobbling. Still perfectly legible and misspelling-free — "worn postmark", not distressed grunge.
- **Copy library — prefer passive-voice printmaking wording** (user rule 2026-09-14; real rubber stamps and postmarks read passive). Pick ONE short line, postmark-sized:
  - With a place: `PRINTED IN NAHA · 2026.09.14` · `HAND-STAMPED AT NAHA · 2026.09.14` · `PULLED BY HAND · NAHA · 2026.09.14`
  - No place / memory photo: `STAMPED IN MEMORY · 2026.09.14` · `PRINTED FROM CHILDHOOD · 2026.09.14` · `KEPT IN INK · 2026.09.14`
  - Bare minimum fallback: `NAHA · 2026.09.14`
- Exact user wording always overrides; omit the place name if uncertain, keep the date.
- The text belongs to the keyline layer, so the keyline separation cell shows the same text at the same position.

## Layer system (flexible)

- Layer count is elastic: default **4 stamping steps** (2x2 grid, see Sheet layout). More steps (up to 8) only on explicit request — and note that accumulation reliability degrades quickly beyond 4.
- **One step may carry SEVERAL colors** (one inking, multi-color — authentic for bingata-style stencils): e.g. the anchor garment prints its base color AND its pattern colors in the same inking. Note such steps explicitly in the prompt ("one inking, three colors").
- Typical decomposition, back to front: background quiet field → ground/road → mid-ground masses (walls, canopy) → secondary greenery → anchor BASE color only (its pattern is NOT here) → **detail inking: the anchor's pattern (bingata 纹样 etc.) + keyline + text — always the FINAL step together**, so the last transition is a big visible jump.
- Each layer is flat geometric shapes in its color family; no gradients, no photographic texture, no fine detail — structure is suggested by shape edges and sparse dashes.
- **Keyline layer** — one near-black (or darkest source color) layer containing ONLY: the text strip and the minimum possible line work (joint marks, subtle fold accents) that keeps the anchor recognizable. NO frame, NO border, NO dashed silhouette outline tracing the outer edge of a figure or object — that reads as a sticker cut line and is forbidden (user-corrected 2026-09-14). Keep keyline work sparse; shapes do the talking.

**Fidelity to the source (hard rule)**: the reduction stays CLOSE to the photo — the anchor's silhouette follows the actual garment/object outline in the photo, walls/trees/ground keep the photo's positions, heights and scale, the figure keeps the photo's proportions. Simplify texture, not geometry: sheet and photo side by side must read as the same picture. In text-only mode, spend prompt words on concrete contour details (garment shape: shoulders/sleeves/hem; wall top edge; horizon height) — vague descriptions are the main cause of contour drift.


## Style presets / 风格预设

Three styles are available: **中性 (Neutral)** — default; **干性 (Dry)**; **油性 (Oily)**.

**Style selection rule**: if the user explicitly names a style (干/中/油, or Dry/Neutral/Oily), always honor it. If unspecified, roll once per job at ratio **干性 : 中性 : 油性 = 2 : 6 : 2** (draw a number 1-10: 1-2 = Dry, 3-8 = Neutral, 9-10 = Oily), then announce the drawn style in the reply before generating.

**中性 (Neutral)**: translucent rubber-stamp ink, grainy streaky fills, hard clean edges, subtle misregistration, realistic overprint.

Inject the chosen style's paragraph at the start of the master prompt and carry its texture into the chained step cells:

- **干性 (Dry)**: screen-print / dry-roller look. Fills are visibly built from a regular **halftone dot screen** (70-100 lpi) on top of a faint **silkscreen mesh/grid texture**; lots of bare-paper specks and thin streaks where the ink ran out; dusty, muted tones; edges are broken, chalky and slightly starved. No soft watercolor edges, no glossy wetness. Keep shapes, knockout voids and overprint logic exactly as in Neutral.
- **油性 (Oily)**: a freshly-inked, richly pressed IMPRESSION on paper — the look left behind by a museum check-in stamp (打卡印记) pressed with plenty of wet ink at the entrance desk. Fills read DENSE, saturated and evenly covering (vs Neutral's grainy streakiness) — err on the GENEROUS side: more ink is more authentic than less (user 2026-09-14, confirmed on the dog+child print); edges stay crisp and flat, with fine roller-grain texture inside each fill. Wet accidents allowed: 1-2 small smudge streaks or fingerprint-like smears at a lower edge where still-wet ink was touched. **NOT an oil painting (verified drift 2026-09-14, three times): "thick paste / pigment accumulation / waxy sheen" vocabulary gets read as oil painting — brushstrokes, impasto, painterly edges. Describe a freshly-inked IMPRESSION, and always attach the NOT-list: no brushstrokes, no impasto, no palette knife, no canvas weave, no painterly rendering, no varnish. Keep shapes, reserved-paper areas and overprint logic as in Neutral.** Other verified drift (2026-09-14, dog+child photo): erasing the background behind subjects on the FINISHED master drifts into plastered-putty slabs (刮腻子) — NEVER region-edit the master to introduce reserved paper; describe unprinted paper areas in the EARLY cells only.

The style can be combined with orientation; any explicit user override always wins over the roll.

**Medium: flat ink, NOT paper cut (hard rule — verified failure mode)**: the most dangerous style drift is 剪纸/paper-craft: layered cut paper with drop shadows, visible paper thickness, felt/cardstock relief. Prevent by (a) always stating the medium positively — "flat opaque printing ink overprinted on warm paper, like a real multi-block woodblock or rubber-stamp overprint" — and (b) an explicit NOT-list: "ABSOLUTELY NOT paper cut, NOT layered paper art, NOT collage, NOT felt craft: no drop shadows under any shape, no visible paper thickness, no physical stacking or depth between color layers, no scissors-cut cardstock edges, no 3D paper relief. Every shape lies on ONE single paper plane; overlapping inks simply darken." Mere "flat geometric shapes" is NOT enough — the watercolor-bloom + paper-texture vocabulary actively pulls the model toward paper collage unless the NOT-list is present. Check Stage A first: if the master drifts to paper-cut, Stage B inherits it.

**Registration**: every layer is slightly misregistered from the others (visible 1-3 mm offsets at this scale, each in its own consistent direction); where two color layers overlap, the ink darkens like real overprint; edges are chalky with fine paper tooth. This misregistration is the aesthetic core — a perfectly aligned result is a failure. (The plate cells show each layer's shapes at their nominal position; only the finished print shows the accumulated offsets.)

**Ink state varies per layer (the craft signature)**: the plates were pulled at different moments, so their ink differs. Give each layer its own state — one freshly inked and dense (richest, most even coverage), one or two pulled with a drier roller (streaky, mottled, bare-paper specks showing through), one medium density with a second-pull thickness, the keyline crisp but unevenly pressured (thin in places, pooling in others). Never render all layers with identical uniform density. The same ink states must carry over into the matching cells.

**Overprint is the defining trait (user-corrected 2026-09-14, three times)**: stamp inks are slightly TRANSLUCENT, never opaque. Where a layer genuinely crosses an EARLIER layer by design (partial overlaps, e.g. foliage over a wall edge), the crossing shows a clearly DARKER MIXED tone (multiply-like: yellow over grey → olive-tinted; red over green → deep brown-red) — the overlap and non-overlap of one shape can never share one color; mixed tones keep crisp union shapes (no soft blending, no pastel averaging). **BUT — knockout reservation for big foreground blocks (the authentic carving logic, user-corrected)**: a large occluding block (anchor garment/figure) is NOT overprinted onto the blocks beneath. The earlier blocks are carved with a silhouette-shaped VOID reserving its space: (a) early cells already show the blank person-shaped silhouette in the road/wall, (b) the foreground block stamps INTO the void as pure color on bare paper with hard butting edges (a thin misregistration paper-sliver along part of the silhouette is welcome), (c) NO translucent mixing between the foreground block and the layers it occludes — it reads "stamped into a reserved hole", never "pasted over, erasing what's below" (the电子绘画 look the user rejected). Within one pull, colors butt or knock out with thin white gaps, never mix. **Voids only shrink (physics rule, user-corrected 2026-09-14)**: reserved blank space can only SHRINK across steps — every void carved in the first pull gets filled by later stamps, and new blank can NEVER appear in a later step; if a block got no void, the only legal later treatment is translucent OVERPRINT on top of the existing ink — never retroactively carving a hole. **What gets a void (user-refined 2026-09-14)**: reserve white ONLY where the later block's colors are too LIGHT to survive overprint (e.g. a white/blue outfit over a dark wall); DARKER blocks overprint translucently DIRECTLY on light under-layers — dark-on-light overprint is authentic and welcome (a golden-brown dog stamps straight over a pale ground, its tone darkening where it crosses the ground ink). **Voids are design-level reserved white (设计留白), NOT paper-cut holes (user-corrected 2026-09-14)**: the under-block ink ends at the void boundary exactly like every other shape edge — hard stamp edge, fine grain; NO white outline, NO gap or halo around the void, no scissors-cut look. Inks stay flat spot colors with HARD clean edges and grainy rubber-stamp texture inside fills — no watercolor blooms, no feathered bleeds, no soft tonal edges. Shape definition comes from the keyline linework and clean edges, never from tonal softness.

## Figures (hard rules, user preferences override)

- Person seen from behind: head is a hair-color shape only (plus hat/accessories in true colors). No face at all.
- Person facing camera: face in a light neutral tone with generalized minimal features only — two short dashes for eyes, one small stroke for a mouth, drawn loosely like ink marks. Never a realistic or beautified face, never a blank void, never a black-filled silhouette.
- Clothing in true source colors. Anonymous compact silhouettes for distant people.

## Composition

- Follow the source photo's arrangement faithfully: subject positions, scale relationships, and horizon stay where the photo puts them — identical in every cell.
- Geometric abstraction with identity preserved: compress buildings, trees, and objects into decisive flat shapes while keeping the anchor recognizable at a glance.

## Prompt skeleton

> A printmaker's simple 4-step stamping diagram derived from the supplied flat block print, printed on warm off-white textured paper, [rolled orientation: 70% square, 30% non-square per the layout rule]. Medium rule: everything is FLAT PRINTING INK on paper like real woodblock overprint - ABSOLUTELY NOT paper cut, NOT layered paper collage, NOT felt craft: no drop shadows, no paper thickness, no stacking depth, no scissor-cut edges; every shape on ONE single paper plane. All four cells copy the supplied print's shapes, colors, contours and composition exactly; no photographic texture. Layout: exactly 4 rectangles of IDENTICAL size in a 2x2 grid with even paper gutters, reading order left-to-right, top-to-bottom. Each cell keeps everything from the previous cell and stamps exactly ONE additional layer, building big under-blocks first and small blocks last: CELL 1 = [large under-blocks: paper field, ground/road, big mid-ground masses]; CELL 2 = cell 1 PLUS [secondary masses]; CELL 3 = cell 2 PLUS [small subject blocks - anchor garment as one block pulling its colors, hair, scattered accents]; CELL 4 = the FINISHED overprint: all layers combined with slight 1-3 mm misregistration per layer and overprint darkening where inks overlap, PLUS the near-black keyline with sparse contour dashes and the small text '[PLACE] - [DATE]' in vintage postal-cancellation capitals (patchy worn rubber postmark ink, legible). Text appears ONLY in cell 4 - no text in cells 1-3. ACCUMULATION IS CRITICAL: never skip or remove earlier layers, never show the finished scene early. ORDER MARKS: a small bold black numeral 1, 2, 3, 4 just outside the UPPER-left corner of each respective rectangle, clearly readable; NO gutter arrows. The prints are plain rectangles: NO border, NO frame, NO rounded corners, no line drawn around them, no dashed underline beneath the text; thin hairline registration crosses just outside two corners of each rectangle. Inks are slightly TRANSLUCENT spot colors with HARD clean edges and grainy rubber-stamp texture inside the fills. Every shape shows two tones: pure ink over bare paper, and a clearly darker mixed tone wherever it crosses an earlier layer (multiply-like overprint) - the overlap and non-overlap of one shape can never share the same color; where shapes collide side by side, use a thin white paper knockout gap. NO watercolor blooms, NO feathered bleeds, NO soft tonal edges. [Figure rules.] No watermark, no other text anywhere.

## Quality checks

- Sheet is a strict reading-order step diagram (default: 2x2, cell 1 largest under-blocks, cell 4 finished overprint); each step = previous cell + exactly one new layer (accumulative, nothing removed); numerals present and correct; sequence unambiguous (no gutter arrows — 2x2 order is self-evident).
- All rectangles IDENTICAL size and orientation, consistent with the rolled/default orientation (or user override); plain prints (no frame, no border, no rounded corners); every step's shapes line up position-for-position with the finished print.
- Steps match the declared layer plan; any multi-color block is printed as one step.
- Fidelity: sheet and source photo read as the same picture (silhouettes, positions, scale).
- Text appears ONLY in the final/finished cell, worn-postmark style, correct and misspelling-free; no dashed underline under it.
- Medium is TRANSLUCENT flat ink with hard edges: no paper-cut drift AND no watercolor drift (no blooms, no feathered bleeds, no soft tonal edges); every shape shows pure ink over paper vs a clearly darker mixed tone over earlier layers — uniform color across both zones is a failure; grainy stamp texture inside fills.
- No dashed outer silhouette outline around figures/objects; keyline is sparse and internal, never a sticker cut-line.
- Misregistration visible in the finished print; overprint darkening where colors overlap; ink density varies between steps.
- Registration crosses and numerals only outside the print areas; no extra text.
- Figure rules respected (no beautified face, no black silhouette).
- No watermark (check bottom-right corner; erase by paper-fill if it shares the bottom band with numerals).

## Delivery

Return the finished image. Briefly name: the identification anchor, the four color layers back-to-front, the keyline content, the ink-state variation chosen, the text strip content, and the saved path.

## WorkBuddy 运行说明（本机适配）

- 文中 "built-in image generation tool" 对应 `ImageGen` 工具；变换照片时把本地图路径传入 `image1`。
- 默认 size 每次随机掷签：70% 方形 `1024x1024`、30% 非方形（竖图出竖版 `1024x1536`、横图出横版 `1536x1024`；方图掷中非方形则按构图选）；用户明示方向则覆盖随机。母版与全部链式格保持同一尺寸。若用户只要单枚成品，只出母版即可。
- 裁水印用托管解释器：`C:\Users\16152\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（已含 Pillow）。**先看水印是否与序号/套准线同处底部条带**：同行则不要裁，用纸色采样 + 微噪点填充水印矩形（注意 Pillow 用实际像素坐标，不要按预览缩放图估算）。
- 本 skill 无脚本依赖，纯 ImageGen 流程；升级时整目录覆盖。
